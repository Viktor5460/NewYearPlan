# Новогодние Планы

Интерактивное расписание новогодних событий в виде писем в почтовом ящике.

## 📦 Что загружать в GitHub?

**ВСЯ ЭТА ПАПКА ЦЕЛИКОМ!**

Просто загрузите всю папку `project-for-github` в GitHub репозиторий.

## 🚀 Как загрузить в GitHub:

1. Создайте репозиторий на GitHub (github.com → New repository)
2. В этой папке выполните команды:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_НИКНЕЙМ/newyear-plans.git
git push -u origin main
```

(Замените `ВАШ_НИКНЕЙМ` и `newyear-plans` на ваши значения)

## 🎯 Что дальше?

После загрузки в GitHub перейдите к папке `render-deploy-instructions` для инструкций по деплою на Render.

⚠️ **Render автоматически возьмет код из GitHub - ничего загружать в Render не нужно!**

## 📁 Структура проекта

- `app.py` - Flask приложение
- `schedule_data.py` - данные о событиях
- `templates/` - HTML шаблоны
- `static/` - CSS и JavaScript файлы
- `requirements.txt` - зависимости Python
- `Procfile` - конфигурация для Render

## 💻 Локальный запуск

```bash
pip install -r requirements.txt
python app.py
```

Сайт будет доступен по адресу: http://localhost:5000
